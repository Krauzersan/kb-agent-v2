# НЕ ИСПОЛЬЗУЕТСЯ. Проект переведён на запуск без Docker (host-режим).
# Запуск см. в README.md (venv + systemd, порт 8745).
# Этот файл оставлен пустым специально, чтобы случайно не поднять контейнеры.
